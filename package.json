{
  "name": "youtube_metadata_extension",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc src/content.ts --noImplicitAny --module commonjs --outDir build && browserify build/content.js -o dist/content.js",
    "lint": "eslint . && prettier --check .",
    "format": "eslint --fix . && prettier --write ."
  },
  "devDependencies": {
    "browserify": "17.0.0",
    "eslint": "8.55.0",
    "eslint-config-prettier": "9.1.0",
    "prettier": "3.1.0",
    "typescript": "5.3.3"
  },
  "dependencies": {
    "date-fns": "2.30.0"
  }
}
